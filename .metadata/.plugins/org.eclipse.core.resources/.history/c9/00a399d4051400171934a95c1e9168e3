package multithreading

import actors.Actor

object ScalaBasicActors {
  
  def main(args: Array[String]): Unit = {
    
    val actor = Actor.actor(
        for (i <- 30 to 100) println(fib(i)))  
    println("Ale Tonto")
        
    
  }
  
  def fib(n: Int): Int = {
    if (n<2) 1 
    else fib(n-1)+fib(n-2)}
  
  
}

