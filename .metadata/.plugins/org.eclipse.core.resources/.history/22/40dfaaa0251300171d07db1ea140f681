package multithreading

object Multithreading {
  
  def main(args: Array[String]): Unit = {
    
    var cnt=0
    for (i <-1 to 10){
      new Thread(new Runnable {
        def run {
          for (j <-1 to 1000000) cnt +=1
        }
      }).start()
    }
    println(cnt)
    
    /*
    new Thread(new Runnable {
      def run {
        for (i <- 1 to 15) {
          println(i)
          Thread.sleep(200)
        }
      }
    }).start
    
    for (c <- 'A' to 'Z'){
      println(c)
      Thread.sleep(100)
    }
   
  */
    
  }
  
  
  
}