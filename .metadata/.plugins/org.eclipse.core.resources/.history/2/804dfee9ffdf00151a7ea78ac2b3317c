package day9.AkkaSampleML

object ParalleCollect extends App {
  
  def fib(n:Int):Int = if (n<2) 1 else fib(n-1)+fib(n-2)
  
  //println(
  
  //for (i <- (30 to 15 by -1).par) {
    ///println(fib(i))
  //}
  
  val i = 0
  for ( i <- 1 to 1000000){
    println(i)
  }
 
  
}