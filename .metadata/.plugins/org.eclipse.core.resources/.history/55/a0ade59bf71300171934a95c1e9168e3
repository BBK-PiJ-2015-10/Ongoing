package multithreading

object ParallelScript {
  
  def main(args: Array[String]): Unit = {
    
    val p = (1 to 100).par.foreach(x=>println(x))
    
    val s = p.seq
    
    //(1 to 100)
  
    println("ale tonto")
    
  }
  
  
  
  
}